// import { artists } from './artistArray.js';

// const artistField = document.querySelectorAll('.current-artist');

// export const currentArtistPlaying = artistField.forEach((artist) => {
//   artist.innerHTML = `now playing: ${artists[0].name}`;
// });
